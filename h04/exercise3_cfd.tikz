\begin{tikzpicture}
\begin{axis}[%
        xlabel=$x$,
        ylabel=$F(x)$,
        grid=both,
        axis x line = bottom,
        axis y line = left,
        jump mark left,
        discontinuous,
    ]
    \addplot table [y=cumulative distribution]{exercise3.dat};
\end{axis}
\end{tikzpicture}