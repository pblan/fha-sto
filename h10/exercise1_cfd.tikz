\begin{tikzpicture}
\begin{axis}[%
        xlabel=$x$,
        ylabel=$H(x)$,
        grid=both,
        axis x line = bottom,
        axis y line = left,
        jump mark left,
        discontinuous,
    ]
    \addplot table [y=cumulative distribution]{exercise1.dat};
\end{axis}
\end{tikzpicture}